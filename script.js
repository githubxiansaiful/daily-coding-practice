const numbers = [1, 2, 3, 4, 5];

// Using forEach to log each number
numbers.forEach(function (number) {
    // console.log(number);
});

numbers.push(7); // Adding a new number to the array
// Using forEach again to log each number after adding a new one
numbers.forEach(function (number) {
    console.log(number);
});

Test